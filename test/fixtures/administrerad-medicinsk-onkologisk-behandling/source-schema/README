# Source schema formats

The .avsc file is the Avro schema used in Kafka for this use case. intEHRgrator
loads `.avsc` as a source-field tree (record fields, not Avro document metadata).
The .json file is a JSON Schema equivalent; instances may carry extra fields
(for example PDL vårdgivare) that are not in the Avro contract.